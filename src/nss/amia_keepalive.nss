//-----------------------------------------------------------------------------
// header
//-----------------------------------------------------------------------------
//script:  amia_keepalive
//group:   core scripts
//used as: Execute script
//date:    sep 13 2007
//author:  Kung
//last:    Faded Wings
//-------------------------------------------------------------------------------
// includes
//-------------------------------------------------------------------------------
//#include "aps_include"
//-------------------------------------------------------------------------------
// main
//-------------------------------------------------------------------------------
void main( ){

    /*// var
    object oModule = GetModule();
    int iHour = GetTimeHour();
    int iMinute = GetTimeMinute();
    int iSecond = GetTimeSecond();
    int iMS = GetTimeMillisecond();
    // package string
    string sTimeString = IntToString(iHour) + "_" + IntToString(iMinute) + "_" + IntToString(iSecond) + "_" + IntToString(iMS);

    // set current time in db
    SetPersistentString(oModule, "TIME", sTimeString, 0, "pwdata");

    SetLocalString( oModule, "NWNX!AMIA!KEEPALIVE", "1" );

    DelayCommand( 300.0, ExecuteScript( "amia_keepalive", oModule ) );*/
}
