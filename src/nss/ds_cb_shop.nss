//-----------------------------------------------------------------------------
// header
//-----------------------------------------------------------------------------
//script:   ds_cb_
//group:    chickenball
//used as:  OnConversation
//date:     2009-07-10
//author:   Disco

//-------------------------------------------------------------------------------
// includes
//-------------------------------------------------------------------------------

#include "inc_ds_cb"

//-------------------------------------------------------------------------------
// main
//-------------------------------------------------------------------------------

void main(){
    /*
    object oPC       = OBJECT_SELF;
    object oTarget   = GetLocalObject( oPC, "ds_target" );
    int nNode        = GetLocalInt( oPC, "ds_node" );

    if ( nNode >= 1 && nNode <= 18 ){

        //18 active tricks
        SetLocalInt( oPC, DS_CB_ACTIVE_TRICK, nNode );
        SendMessageToPC( oPC, "Setting active trick "+IntToString( nNode ) );
    }
    else if ( nNode == 19 ){

        //remove active trick
        DeleteLocalInt( oPC, DS_CB_ACTIVE_TRICK );
        SendMessageToPC( oPC, "Deleting active trick " );
    }
    else if ( nNode >= 21 && nNode <= 38 ){

        //18 passive tricks
        SetLocalInt( oPC, DS_CB_PASSIVE_TRICK, nNode );
        SendMessageToPC( oPC, "Setting passive trick "+IntToString( nNode ) );
    }
    else if ( nNode == 39 ){

        //remove passive trick
        DeleteLocalInt( oPC, DS_CB_PASSIVE_TRICK );
        SendMessageToPC( oPC, "Setting passive trick " );
    }
    */
}

