void main(){

    int nPCs      = 0;
    int nSwitch   = 0;
    object oPC    = GetFirstPC();

    while (GetIsObjectValid(oPC) == TRUE){

        nPCs = nPCs+1; // nPCs++;

        if ( nSwitch == 1 ){


        }


        oPC  = GetNextPC();

    }

}


